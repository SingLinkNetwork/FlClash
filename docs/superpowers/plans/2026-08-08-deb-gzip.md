# #1029 DEB gzip 壓縮修復計畫

## 1. 建立失敗測試

- 新增 gzip/zstd/缺成員的 ar fixture 測試。
- 先確認新測試在目前程式上失敗。

## 2. 實作打包修復

- 修改固定的 `flutter_distributor` patch，讓 `dpkg-deb` 明確使用 gzip。
- 保持其他 Linux 產物與既有 StartupWMClass patch 不變。

## 3. 接入 CI 驗證

- 新增獨立 `.deb` 壓縮格式 verifier。
- 在 Linux packaging job 產出後執行 verifier。
- 在靜態 source matrix 中檢查 patch、verifier 與 CI wiring，並讓 `build` 依賴 Linux packaging。

## 4. 驗證與交付

- 跑本機 Ruby 測試、靜態驗證、YAML 解析、Dart/Go 基線檢查。
- 推送後等待完整跨平台 CI/CD 與最後總閘門。
- 建立並關閉下游 #1029 記錄，之後才選下一個上游問題。
